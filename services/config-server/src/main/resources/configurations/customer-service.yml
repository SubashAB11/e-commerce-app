spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer?currentSchema=public&stringtype=unspecified
    username: subash
    password: subash
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
